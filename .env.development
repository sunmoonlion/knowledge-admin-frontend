VITE_PWA_DEBUG=true
VITE_MOCK_ENABLE=false

# 不要在本文件写 VITE_API_URL，否则会覆盖根目录 .env（pnpm dev 用 development 模式会加载本文件）。
# 管理端 BFF 必须是 knowledge-admin-backend（端口 8001）；不要写 8000（knowledge-web-backend），否则登录会走 Web，回调后跳到 3000。
